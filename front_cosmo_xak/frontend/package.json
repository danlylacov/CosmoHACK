{
  "name": "eva-decision-support",
  "private": true,
  "version": "0.9.2",
  "type": "module",
  "description": "Прототип поддержки решений при планировании ВКД на МКС — КосмоХакатон 2026",
  "scripts": {
    "test": "vitest run",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "mocks": "node scripts/generate-mocks.mjs",
    "msw:init": "msw init public --save"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "msw": "^2.7.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "vite": "^6.0.0"
  },
  "msw": {
    "workerDirectory": [
      "public"
    ]
  }
}
